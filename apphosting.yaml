# apphosting.yaml  (committed to git — NO secrets here)
runConfig:
  cpu: 1
  memoryMiB: 512
  minInstances: 0      # scale-to-zero saves cost on a demo
  maxInstances: 1
  concurrency: 80

env:
  - variable: NODE_ENV
    value: production
    availability:
      - BUILD
      - RUNTIME
